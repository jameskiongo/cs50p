class Vehicle:
    def __init__(self, name, max_speed, mileage):
        self.max_speed = max_speed
        self.mileage = mileage
        self.name = name

# class Bus(Vehicle):
   # pass
    def seating_capacity(self,capacity=50):
        return f""
school_bus = Bus("volvo", 180, 12)

print(f"{school_bus.name}")

